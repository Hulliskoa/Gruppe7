<!DOCTYPE html>
<html>
<head>
	<link rel="stylesheet" href="css/style.css"/>
	<link rel="stylesheet" href="css/dashboard.css"/>
	<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Ubuntu&display=swap">
	<title>Ultra - Projects</title>
</head>
<body>
	<header>
        <div class="head-container">
            <div class="head-column">
				<!--ORDENTLIG LINK MÅ INN I HREF-->
                <a href="/dashboard.ejs"><img id="logo-icon" src="/img/logo.png" alt="Logo"></a>
            </div>
            <div class="head-column">
            </div>
            <div class="head-column">
            </div>
            <div class="head-column">
				<div class="dropdown">
				  <img class="dropbtn" id="profilePicture" src="<%=profilePicture%>">
				  <div class="dropdown-content">
					<a class="dropdown-li" href="https://github.com/Hessh/Test-repo">Github</a>
					<a class="dropdown-li" href="https://trello.com/b/FLL4lMIf/prosjektstyringsapp">Trello</a>
					<a class="dropdown-li" href="https://www.figma.com/file/WSdBuMBUvZqwTLJ0fI3OjRKm/Gruppe-7?node-id=6%3A2">Figma</a>
					<a class="dropdown-li" href="/logout">Sign out</a>
				  </div>
				</div> 
            </div>
        </div>  
    </header>
	<main>
		<div class="container">

			<div class="column column-1">
			</div>

			<!----------- 
			EJS file allow us to use javascript within the HTML tags to populate with data from the arrays we send from server to client
			------------->
			<div class="column column-2">
				<%let repoName;%>
				<%let columnClass;%>
				<%let user = userName;%>
				<%let owned = "owned"%>
				<%let colab = "colab"%>
				<%let repoID = 1%>
				<% for(let i = 0; i < repoNames.length; i++){ %>
					<%repoName = repoNames[i].name%>
					<%if (repoNames[i].owner == user){%>
						<div class="item" onclick="post('<%=repoID + owned%>')">
							<div class="content">
								<h4><%=repoNames[i].name%></h4>
								<h5>Name</h5>
								<p>Innhold</p>
								<form id=<%=repoID + owned%> action="/inputName" method="POST">
									<input type="hidden" name="repo" value=<%=repoNames[i].name%>>
								</form>
							</div>			                       
						</div>
					<%}%>
					<%repoID++%>
				<%}%>
			</div>	

			<div class="column column-3">
				<%repoID = 1%>
				<% for(let i = 0; i < repoNames.length; i++){ %>
					<%repoName = repoNames[i].name%>
					<%if (repoNames[i].owner != user){%>
						<div class="item" onclick="post('<%=repoID + colab%>')">
							<div class="content">
								<h4><%=repoNames[i].name%></h4>
								<h5>Name</h5>
								<p>Innhold</p>
								<form id=<%=repoID + colab%> action="/inputName" method="POST">
									<input type="hidden" name="repo" value=<%=repoNames[i].name%>>
								</form>
							</div>			                       
						</div>
					<%}%>
					<%repoID++%>
				<%}%>
			</div>

			<div class="column column-4">
			</div>

		</div>
	</main>
	<footer></footer>
	
	<script type="text/javascript" src="scripts/dashboard.js"></script>
</body>
</html>